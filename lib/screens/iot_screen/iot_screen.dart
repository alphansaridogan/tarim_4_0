import 'package:flutter/material.dart';

class IotScreen extends StatelessWidget {
  const IotScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Tarım 4.0'),
      ),
    );
  }
}